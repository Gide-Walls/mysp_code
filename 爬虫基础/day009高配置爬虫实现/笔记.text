不推荐多线程的原因
    资源
线程共享全局变量    资源竞争 gil解析锁  python是一个伪多线程

多进程 不共享全局变量

资源共享用队列 

用那个任务
    反爬很严重
        ip 账号(充足) 异步
        没有那么多 ip 账号紧缺 线程 
            协程太快 的话用线程 温柔一点


异步
    不需要通信的 比如下载网页 
        发送请求 要等待
            request 是同步的仅仅支持
                aiohttp 支持异步
                    async 用来声明一个函数是异步函数
                        await 用来挂起
异步的状态
    等待   运行 完成
